<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">


    <ImageView
        android:layout_marginStart="15dp"
        android:layout_marginTop="20dp"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:id="@+id/exchange_logo_iv"
        android:src="@mipmap/bar_icon_back_white"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/exchange_name_tv"
        app:layout_constraintTop_toTopOf="@id/exchange_logo_iv"
        app:layout_constraintLeft_toRightOf="@id/exchange_logo_iv"
        android:textColor="@color/white"
        android:textSize="14sp"
        tools:text="Binance(币安)"
        android:layout_marginStart="10dp"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/exchange_info_tv"
        android:layout_marginStart="10dp"
        app:layout_constraintBottom_toBottomOf="@id/exchange_logo_iv"
        app:layout_constraintLeft_toRightOf="@id/exchange_logo_iv"
        android:textColor="@color/profit_tab_text_unselect"
        android:textSize="12sp"
        tools:text="支持导入资产和交易记录"
        />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/exchange_logo_iv"
        app:layout_constraintBottom_toBottomOf="@id/exchange_logo_iv"
        tools:src="@drawable/ic_launcher_foreground"
        android:layout_marginEnd="30dp"
        />

    <View
        android:layout_width="match_parent"
        android:layout_height="0.3dp"
        android:layout_marginStart="15dp"
        android:layout_marginEnd="15dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/exchange_logo_iv"
        android:layout_marginTop="10dp"
        android:background="@color/profit_tab_text_unselect"
        />


</android.support.constraint.ConstraintLayout>